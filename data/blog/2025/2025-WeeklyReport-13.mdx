---
title: 2025 Weekly Report 13
date: '2025-03-30'
tags: ['Weekly Report']
draft: false
images: []
summary: 2025第13周周报
---

<TOCInline toc={props.toc}/>

## 工作

- ### 1 Nestjs

`node nestjs` 有点像 `java springboot`，上手起来非常快，2年多没写`Java`，重新拾起 `aop/di`

------------

- ### 2 Code Agent

这周用 `devv` 的 `builder` 模式（agent mode），想构建一个 `nestjs demo` 项目，功能只要`用户管理`和`鉴权`即可，

结果幻觉严重，`nestjs` 写着写着就变成 `hono` 了，修改 `promt` 强制使用 `nestjs` 后，又改了回来，结果修改两次之后又忘记上下文了，又开始用 `hono` 构建了，暂时放弃这个 agent 了

最后用了一个 [nestjs starter](https://github.com/rodion-arr/nestjs-starter-kit) 当脚手架

------------



## 生活

- **1** 健身周报：一周5练，不想练，乏力
- **2** 做菜周报：花 50+ 买了条桂鱼，做了鲜香美味的鱼汤（配菜：豆腐/鸡蛋/紫苏）
- **3** 长沙天气满30减20，这周末出门买菜直接下冰雹，砸头上才反应过来
- **4** 持有 `#APLD` ，`NVDA H20影响` + `MSFT 削减数据中心支出`，导致我血亏，但 conviction 依然存在


## 其他









